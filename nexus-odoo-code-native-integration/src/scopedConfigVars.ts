// This integration configures its Odoo connection on the deployment wizard's
// Configuration page. Keep scoped config vars empty so the same key is not
// registered a second time as a reusable organization/customer connection.
export const scopedConfigVars = {};
